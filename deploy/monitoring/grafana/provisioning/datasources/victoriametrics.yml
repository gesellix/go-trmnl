apiVersion: 1

datasources:
  - name: VictoriaMetrics
    uid: victoriametrics
    type: prometheus
    access: proxy
    url: http://victoriametrics:8428
    isDefault: true
    jsonData:
      # Match the scrape interval so graphs do not invent resolution.
      timeInterval: 60s
      httpMethod: POST
